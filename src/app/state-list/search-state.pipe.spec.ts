import { SearchStatePipe } from '../pipes/search-state.pipe';

describe('SearchStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStatePipe();
    expect(pipe).toBeTruthy();
  });
});
